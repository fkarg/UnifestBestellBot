[project]
name = "unifestbestellbot"
version = "0.2.0"
description = "Telegram bot for stalls at the Karlsruhe Unifest to order supplies"
authors = [
    { name = "Felix Karg", email = "f.karg10@gmail.com" },
]
requires-python = ">=3.14"
license = { text = "GPL-3.0-or-later" }
readme = "README.md"

dependencies = [
    "aiogram>=3.13,<4.0",
    "sqlmodel>=0.0.22,<0.1",
    "pydantic>=2.9,<3.0",
    "pydantic-settings>=2.6,<3.0",
    "fastapi>=0.115,<0.120",
    "uvicorn[standard]>=0.32,<0.40",
    "httpx>=0.28,<0.29",
    "pyyaml>=6.0,<7.0",
]

[project.scripts]
unifestbestellbot = "unifestbestellbot.__main__:run"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/unifestbestellbot"]

[dependency-groups]
dev = [
    "pytest>=8.3,<9.0",
    "pytest-asyncio>=0.24,<0.30",
    "pytest-cov>=6.0,<7.0",
    "ruff>=0.8,<0.20",
    "ty>=0.0.38",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["src"]
filterwarnings = [
    "ignore::DeprecationWarning:pydantic.*",
]

[tool.ruff]
line-length = 100
target-version = "py314"
src = ["src/unifestbestellbot", "tests"]

[tool.ty.src]
include = ["src/unifestbestellbot"]

[tool.ty.rules]
# pydantic-settings reads field values from the environment at runtime;
# ty can't statically prove the required fields are provided.
missing-argument = "ignore"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501"]
